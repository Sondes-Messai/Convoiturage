class Adress {
  
  constructor(id, latitude, longitude,  road, town, zipCode, typeAddress) {
    (this.id = id),
    (this.latitude = latitude),
    (this.longitude = longitude),
    (this.road = road),
    (this.town = town),
    (this.zipCode = zipCode),
    (this.typeAddress = typeAddress);
  }
}

export default Adress;
